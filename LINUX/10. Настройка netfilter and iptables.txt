ptables €вл€етс€ утилитой, выполн€ющей функции межсетевого экрана. 
≈е настройка производитс€ в командной строке, с помощью правил iptables 
можно разрешать или блокировать прохождение трафика.  огда происходит 
попытка установлени€ соединени€ с текущей машиной, iptables просматривает 
список правил в списке, чтобы пон€ть, 
как нужно поступить в этом случае. ≈сли правила нет, то выполн€етс€ 
действие по умолчанию.

sudo apt-get install iptables

ѕравило ниже позвол€ет заблокировать все соединени€ с IP-адреса 10.10.10.10:

iptables -A INPUT -s 10.10.10.10 -j DROP

ѕравило ниже позвол€ет заблокировать соединени€ из диапазона IP-адресов 10.10.10.0/24

iptables -A INPUT -s 10.10.10.0/24 -j DROP

Ќиже показано, как заблокировать SSH-соединени€ с хоста 10.10.10.10:

iptables -A INPUT -p tcp --dport ssh -s 10.10.10.10 -j DROP

¬от так можно заблокировать SSH-соединени€ с любого IP-адреса.

iptables -A INPUT -p tcp --dport ssh -j DROP

